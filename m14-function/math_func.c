#include <stdio.h>
#include <math.h>

int main()
{
    int n = 4.2;
    // int ans = ceil(4.2);
    // int ans = floor(4.2);
    // int ans = round(4.2);
    // double ans = sqrt(25);
    // int ans = pow(3, 2);
    int ans = abs(-3);
    printf("%d", ans);
    return 0;
}